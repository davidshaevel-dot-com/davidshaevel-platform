# AWS Configuration for Project
# Copy this file to .envrc and update with your actual values
#
# IMPORTANT: Never commit .envrc to Git!
# The .envrc file contains your actual AWS account ID and project name.
#
# Usage:
#   1. cp .envrc.example .envrc
#   2. Update the values below with your actual AWS account and project details
#   3. source .envrc
#
# Optional: Install direnv for automatic loading
#   brew install direnv
#   echo 'eval "$(direnv hook zsh)"' >> ~/.zshrc
#   direnv allow

# AWS Profile (uses SSO or configured profile)
export AWS_PROFILE=your-profile-name

# AWS Region (used by AWS CLI and Terraform AWS provider)
export AWS_REGION=us-east-1

# Project Variables
# Replace with your actual 12-digit AWS account ID
export TF_VAR_aws_account_id="123456789012"

# Replace with your project name (lowercase, no spaces)
export TF_VAR_project_name="myproject"

# Environment
export TF_VAR_environment="dev"

# Domain name - Replace with your actual domain name
export TF_VAR_domain_name="example.com"

# Cloudflare Configuration (for grafana-dns-switch.sh script)
# Get API token from: https://dash.cloudflare.com/profile/api-tokens
# Create token with Zone:DNS:Edit permissions for your domain
export CLOUDFLARE_API_TOKEN="your-cloudflare-api-token"

# Get Zone ID from: Cloudflare Dashboard > Your domain > Overview (right sidebar)
export CLOUDFLARE_ZONE_ID="your-zone-id"

# Neon Database Configuration (for Vercel deployment)
# Create a free database at: https://neon.tech
# Get connection string from: Neon Console > Your Project > Connection Details
export NEON_DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"
